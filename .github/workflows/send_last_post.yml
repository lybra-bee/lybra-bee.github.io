name: 📢 Publish Last Post to Telegram

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "_posts/**"

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 📦 Install dependencies
        run: |
          pip install PyYAML requests

      - name: 🚀 Extract Last Post and Send to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          import os, glob, yaml, re, requests

          # Находим последний пост
          posts = sorted(glob.glob('_posts/*.md'), reverse=True)
          if not posts:
              raise SystemExit("❌ Нет файлов в _posts/")
          last_post = posts[0]

          with open(last_post, encoding='utf-8') as f:
              content = f.read()

          # Контент без front-matter
          parts = content.split('---')
          body = parts[-1].strip() if len(parts) > 1 else content.strip()

          # Тизер: первые 30 слов
          words = body.split()
          teaser = ' '.join(words[:30]) + ('…' if len(words) > 30 else '')

          # Экранирование MarkdownV2
          def esc(text):
              return re.sub(r'([_*\[\]()~`>#+\-=|{}.!])', r'\\\1', text)

          teaser_esc = esc(teaser)
          hashtags_esc = esc("#ИИ #LybraAI")

          # Пытаемся правильно парсить front-matter
          front_matter = {}
          if len(parts) > 1:
              try:
                  front_matter = yaml.safe_load(parts[1])
              except yaml.YAMLError as e:
                  print(f"❌ Ошибка в front-matter: {e}")
          
          # Отладка: Проверка содержимого front-matter
          print("Front-matter:", front_matter)

          # Изображение из front-matter
          image_name = os.path.basename(front_matter.get('image', 'post-1.png'))
          image_url = f"https://lybra-bee.github.io/assets/images/posts/{image_name}"

          # Проверка доступности изображения
          try:
              image_check = requests.get(image_url)
              if image_check.status_code == 200:
                  print(f"✅ Изображение доступно: {image_url}")
              else:
                  print(f"❌ Изображение недоступно: {image_url}")
          except requests.exceptions.RequestException as e:
              print(f"❌ Ошибка при проверке изображения: {str(e)}")

          # Проверка расширения изображения (должно быть .png или .jpg)
          allowed_extensions = ['.jpg', '.jpeg', '.png']
          if not any(image_url.endswith(ext) for ext in allowed_extensions):
              raise SystemExit(f"❌ Неверный формат изображения: {image_url}")

          # Статическая ссылка на сайт
          site_url = "https://lybra-ai.ru"

          # Формируем сообщение
          message = f"*Новая статья*\n\n{teaser_esc}\n\n[Читать на сайте]({site_url})\n\n{hashtags_esc}"

          # Проверка токена и чата
          token = os.getenv('TELEGRAM_BOT_TOKEN')
          chat_id = os.getenv('TELEGRAM_CHAT_ID')
          if not token or not chat_id:
              raise SystemExit("❌ Нет TELEGRAM_BOT_TOKEN или TELEGRAM_CHAT_ID")

          # Отправка фото с подписью в Telegram
          resp = requests.post(
              f"https://api.telegram.org/bot{token}/sendPhoto",
              data={
                  "chat_id": chat_id,
                  "photo": image_url,
                  "caption": message,
                  "parse_mode": "MarkdownV2"
              }
          )

          print(f"✅ Telegram ответ: {resp.status_code} — {resp.text[:200]}")
        shell: python
