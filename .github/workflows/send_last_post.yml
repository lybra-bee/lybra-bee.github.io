name: 📢 Publish Last Post to Telegram

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "_posts/**"

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 📦 Install dependencies
        run: |
          pip install PyYAML requests

      - name: 🚀 Extract Last Post and Send to Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          import os, glob, yaml, re, requests

          # Находим последний пост
          posts = sorted(glob.glob('_posts/*.md'), reverse=True)
          if not posts:
              raise SystemExit("❌ Нет файлов в _posts/")
          last_post = posts[0]

          with open(last_post, encoding='utf-8') as f:
              content = f.read()

          # Front-matter
          front_matter = re.search(r'^---(.*?)---', content, re.S)
          if not front_matter:
              raise SystemExit("❌ Не найден front matter в последнем посте")
          meta = yaml.safe_load(front_matter.group(1))

          title = meta.get('title', 'Без названия')
          date = str(meta.get('date', ''))
          slug = os.path.splitext(os.path.basename(last_post))[0].split('-', 3)[-1]

          # Тизер
          teaser = meta.get('description', '').strip()
          if not teaser:
              # Берём первое предложение контента
              body = content.split('---')[-1].strip()
              first_sentence = re.split(r'[.!?]', body)[0].strip()
              teaser = first_sentence if len(first_sentence) > 30 else "Читайте новую статью о трендах ИИ 2025 года на нашем сайте!"
          teaser = teaser[:200] + '…' if len(teaser) > 200 else teaser

          # Картинка
          base_url = "https://lybra-bee.github.io/assets/images/posts/"
          image_name = f"{os.path.basename(meta.get('image', 'post-1.png'))}"
          image_url = base_url + image_name

          # URL поста
          url = f"https://lybra-bee.github.io/{date[:10].replace('-', '/')}/{slug}.html"

          # Экранирование MarkdownV2
          def esc(text):
              return re.sub(r'([_*\[\]()~`>#+\-=|{}.!])', r'\\\1', text)

          title_esc = esc(title)
          teaser_esc = esc(teaser)
          hashtags_esc = esc("#ИИ #LybraAI")

          message = f"📢 *Новый пост*: \"{title_esc}\"\n\n*Краткий тизер*: {teaser_esc}\n\n[Читать статью]({url})\n\n{hashtags_esc}"

          # Проверка токена и чата
          token = os.getenv('TELEGRAM_BOT_TOKEN')
          chat_id = os.getenv('TELEGRAM_CHAT_ID')
          if not token or not chat_id:
              raise SystemExit("❌ Нет TELEGRAM_BOT_TOKEN или TELEGRAM_CHAT_ID")

          # Отправка фото с подписью
          resp = requests.post(
              f"https://api.telegram.org/bot{token}/sendPhoto",
              data={
                  "chat_id": chat_id,
                  "photo": image_url,
                  "caption": message,
                  "parse_mode": "MarkdownV2"
              }
          )

          print(f"✅ Telegram ответ: {resp.status_code} — {resp.text[:200]}")
        shell: python
