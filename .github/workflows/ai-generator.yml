name: AI Content Generator

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * 1'  # Каждый понедельник в 12:00 UTC

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run content generator with cleanup
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          echo "=== ЗАПУСК ГЕНЕРАТОРА КОНТЕНТА С ОЧИСТКОЙ ==="
          echo "🧹 Будет сохранено только 3 последние статьи"
          python .github/scripts/generate_content.py
          echo "=== ГЕНЕРАЦИЯ И ОЧИСТКА ЗАВЕРШЕНЫ ==="

      - name: Commit and push changes
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          
          echo "📊 Текущее состояние файлов:"
          find content/posts/ -name "*.md" -exec ls -la {} \; | sort -k8,8 | tail -5
          
          git add content/posts/
          
          if git diff --staged --quiet; then
            echo "❌ Нет изменений для коммита"
            exit 0
          fi
          
          git commit -m "🤖 AI статья + очистка: $(date +"%d.%m.%Y %H:%M")"
          git pull --rebase
          git push
          echo "✅ Изменения запушены!"

      - name: Final report
        run: |
          echo "=== ФИНАЛЬНЫЙ ОТЧЕТ ==="
          echo "📋 Последние 5 статей (всего сохраняется 3):"
          find content/posts/ -name "*.md" -exec basename {} \; | sort -r | head -5
          echo "✅ Всего статей после очистки: $(find content/posts/ -name "*.md" | wc -l)"
