name: 🚀 Автогенерация контента и публикация

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * *" # каждый день в 9:00 по UTC

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Устанавливаем Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # 3. Устанавливаем зависимости
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Запускаем генерацию статьи и изображения
      - name: Generate content
        env:
          CLIPDROP_API_KEY: ${{ secrets.CLIPDROP_API_KEY }}
        run: |
          echo "🚀 Генерация..."
          python content_generator.py

      # 5. Собираем сайт Hugo
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "0.128.1"

      - name: Build site
        run: hugo --minify

      # 6. Публикуем на GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
