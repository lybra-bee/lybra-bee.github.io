name: AI Content Generator

on:
  workflow_dispatch:
    inputs:
      article_count:
        description: 'Number of articles to generate'
        required: true
        default: '1'
        type: choice
        options:
        - '1'
        - '2'
        - '3'
      custom_prompt:
        description: 'Custom prompt for Colab'
        required: false
        default: 'Генерация AI контента для блога'
  schedule:
    - cron: '0 8 * * *'  # Каждый день в 8:00 UTC

permissions:
  contents: write

jobs:
  health-check:
    runs-on: ubuntu-latest
    outputs:
      webhook_status: ${{ steps.health-check.outputs.status }}
    steps:
    - name: 🔍 Health Check Webhook
      id: health-check
      env:
        WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      run: |
        echo "🔍 Проверяю доступность webhook..."
        response=$(curl -s -X GET "$WEBHOOK_URL/api/test" || echo "failed")
        
        if echo "$response" | grep -q "success"; then
          echo "✅ Webhook endpoint доступен"
          echo "status=success" >> $GITHUB_OUTPUT
        else
          echo "❌ Webhook endpoint недоступен"
          echo "Response: $response"
          echo "status=failed" >> $GITHUB_OUTPUT
        fi

  activate-colab:
    runs-on: ubuntu-latest
    needs: health-check
    if: needs.health-check.outputs.webhook_status == 'success'
    outputs:
      activation_status: ${{ steps.activate.outputs.status }}
    
    steps:
    - name: 🚀 Activate Colab
      id: activate
      env:
        ACTIVATION_SECRET: ${{ secrets.ACTIVATION_SECRET }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      run: |
        # Определяем тип запуска и промпт
        if [ "${{ github.event_name }}" = "schedule" ]; then
          PROMPT="Плановая генерация контента: AI и технологии $(date +'%Y-%m-%d')"
          ACTION_TYPE="scheduled"
        else
          PROMPT="${{ github.event.inputs.custom_prompt }}"
          ACTION_TYPE="manual"
        fi
        
        echo "🚀 Активирую Colab: $PROMPT"
        echo "🌐 Webhook URL: $WEBHOOK_URL"
        echo "🔐 Secret: ${ACTIVATION_SECRET:0:10}..."
        echo "👤 Chat ID: $TELEGRAM_CHAT_ID"
        
        # Отправляем запрос активации
        echo "📨 Отправляю запрос активации..."
        response=$(curl -s -w "\n%{http_code}" -X POST "$WEBHOOK_URL/api/activate-colab" \
          -H "Content-Type: application/json" \
          -d '{
            "prompt": "'"$PROMPT"'",
            "secret": "'"$ACTIVATION_SECRET"'",
            "action_type": "'"$ACTION_TYPE"'",
            "chat_id": "'"$TELEGRAM_CHAT_ID"'"
          }')
        
        # Разделяем response и status code
        http_code=$(echo "$response" | tail -n1)
        response_body=$(echo "$response" | sed '$d')
        
        echo "📋 HTTP Code: $http_code"
        echo "📋 Response: $response_body"
        
        # Ждем активации Colab
        echo "⏳ Ожидаю активации Colab (40 сек)..."
        sleep 40
        
        # Проверяем статус активации
        if [ "$http_code" = "200" ] && echo "$response_body" | grep -q "success"; then
          echo "✅ Colab активирован успешно"
          echo "status=success" >> $GITHUB_OUTPUT
        else
          echo "❌ Ошибка активации Colab"
          echo "status=failed" >> $GITHUB_OUTPUT
          
          # 🔧 Альтернатива: прямая отправка сообщения
          echo "🔄 Пробую прямую отправку сообщения..."
          direct_response=$(curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -H "Content-Type: application/json" \
            -d '{
              "chat_id": "'"$TELEGRAM_CHAT_ID"'",
              "text": "/generate '"$PROMPT"'",
              "parse_mode": "Markdown"
            }')
          
          echo "📋 Direct response: $direct_response"
        fi

    - name: 📨 Уведомление о запуске
      if: always()
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        if [ "${{ job.status }}" = "success" ]; then
          MESSAGE="🚀 Colab активирован! Запускаю генерацию контента..."
        else
          MESSAGE="❌ Ошибка активации Colab. Проверьте логи."
        fi
        
        curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
          -H "Content-Type: application/json" \
          -d "{\"chat_id\": \"$TELEGRAM_CHAT_ID\", \"text\": \"$MESSAGE\"}"

  generate-content:
    runs-on: ubuntu-latest
    needs: activate-colab
    if: needs.activate-colab.outputs.activation_status == 'success'
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 Install dependencies
      run: |
        pip install requests pillow python-dotenv

    - name: 📁 Create content directories
      run: |
        mkdir -p content/posts
        mkdir -p static/images/posts

    - name: 🤖 Generate AI content
      env:
        OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        ACTIVATION_SECRET: ${{ secrets.ACTIVATION_SECRET }}
        WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      run: |
        echo "🔄 Запуск генератора контента..."
        echo "📁 Текущая директория: $(pwd)"
        
        if [ -f ".github/scripts/generate_content.py" ]; then
            echo "✅ Найден .github/scripts/generate_content.py"
            python .github/scripts/generate_content.py \
              --token "$TELEGRAM_BOT_TOKEN" \
              --chat-id "$TELEGRAM_CHAT_ID" \
              --secret "$ACTIVATION_SECRET" \
              --webhook "$WEBHOOK_URL" \
              --count ${{ github.event.inputs.article_count }} \
              --debug
        else
            echo "❌ Файл не найден!"
            find . -name "*.py" -type f
            exit 1
        fi

    - name: 💾 Commit and push generated content
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Обновляем локальную копию
        git fetch origin
        git reset --hard origin/main
        
        # Добавляем только новые файлы
        git add content/posts/
        git add static/images/posts/
        
        # Коммит если есть изменения
        if ! git diff --cached --quiet; then
            git commit -m "Auto-generated AI content $(date +'%Y-%m-%d %H:%M')"
            git push origin main
            echo "✅ Изменения отправлены успешно"
        else
            echo "✅ Нет новых изменений для коммита"
        fi

  setup-webhook:
    runs-on: ubuntu-latest
    needs: generate-content
    if: success()
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4

    - name: 🤖 Setup Telegram Webhook
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      run: |
        echo "🔄 Setting up webhook..."
        response=$(curl -s -X POST \
          "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/setWebhook" \
          -H "Content-Type: application/json" \
          -d "{\"url\": \"$WEBHOOK_URL/api/telegram\", \"max_connections\": 100, \"drop_pending_updates\": true}")
        
        echo "📋 Webhook response: $response"
        
        echo "🔍 Webhook status:"
        curl -s "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/getWebhookInfo" | jq .

  notify-completion:
    runs-on: ubuntu-latest
    needs: generate-content
    if: always()
    
    steps:
    - name: 📨 Send completion notification
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        if [ "${{ job.status }}" = "success" ]; then
          MESSAGE="✅ Генерация контента завершена успешно! $(date +'%Y-%m-%d %H:%M')"
        else
          MESSAGE="❌ Ошибка генерации контента. Статус: ${{ job.status }}"
        fi
        
        curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
          -H "Content-Type: application/json" \
          -d "{\"chat_id\": \"$TELEGRAM_CHAT_ID\", \"text\": \"$MESSAGE\"}"

  fallback-direct:
    runs-on: ubuntu-latest
    needs: health-check
    if: needs.health-check.outputs.webhook_status == 'failed'
    
    steps:
    - name: 🚀 Direct Activation Fallback
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        echo "🔄 Использую прямое подключение (fallback)..."
        
        if [ "${{ github.event_name }}" = "schedule" ]; then
          PROMPT="Плановая генерация: AI технологии $(date +'%Y-%m-%d')"
        else
          PROMPT="${{ github.event.inputs.custom_prompt }}"
        fi
        
        curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
          -H "Content-Type: application/json" \
          -d '{
            "chat_id": "'"$TELEGRAM_CHAT_ID"'",
            "text": "/generate '"$PROMPT"'",
            "parse_mode": "Markdown"
          }'
        
        echo "✅ Сообщение отправлено напрямую"
