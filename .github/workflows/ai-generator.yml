name: Generate and Deploy Hugo Site

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests python-slugify pyyaml

    - name: Create necessary directories
      run: |
        mkdir -p content/posts
        mkdir -p static/images/posts
        mkdir -p data
        mkdir -p layouts/partials
        mkdir -p layouts/articles
        mkdir -p layouts/gallery
        mkdir -p layouts/about
        mkdir -p layouts/_default

    - name: Create config.toml
      run: |
        cat > config.toml << 'EOF'
        baseURL = "https://${{ github.repository_owner }}.github.io/"
        languageCode = "ru-ru"
        title = "AI Портфолио"

        [params]
        description = "Портфолио сгенерированных AI статей и изображений"

        [menu]
          [[menu.main]]
            name = "Главная"
            url = "/"
            weight = 1

          [[menu.main]]
            name = "Статьи"
            url = "/articles/"
            weight = 2

          [[menu.main]]
            name = "Галерея"
            url = "/gallery/"
            weight = 3

          [[menu.main]]
            name = "Обо мне"
            url = "/about/"
            weight = 4

        [outputs]
          home = ["HTML", "RSS"]
        EOF

    - name: Create layout files
      run: |
        # Header partial
        cat > layouts/partials/header.html << 'EOF'
        <nav class="bg-white shadow-sm sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between items-center h-16">
                    <div class="text-xl font-bold text-gray-800">
                        <a href="/">🎨 AI Портфолио</a>
                    </div>
                    <div class="hidden md:flex space-x-8">
                        <a href="/" class="text-gray-700 hover:text-blue-600 transition">Главная</a>
                        <a href="/articles/" class="text-gray-700 hover:text-blue-600 transition">Статьи</a>
                        <a href="/gallery/" class="text-gray-700 hover:text-blue-600 transition">Галерея</a>
                        <a href="/about/" class="text-gray-700 hover:text-blue-600 transition">Обо мне</a>
                    </div>
                </div>
            </div>
        </nav>
        EOF

        # Footer partial
        cat > layouts/partials/footer.html << 'EOF'
        <footer class="bg-gray-800 text-white py-12">
            <div class="max-w-7xl mx-auto px-4 text-center">
                <p class="text-lg mb-4">🎨 AI Портфолио</p>
                <p class="text-gray-400">Сгенерировано с помощью AI технологий</p>
                <div class="mt-6">
                    <p class="text-sm text-gray-500">© 2024 Все права защищены</p>
                </div>
            </div>
        </footer>
        EOF

        # Single post layout
        cat > layouts/_default/single.html << 'EOF'
        <!DOCTYPE html>
        <html lang="ru">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>{{ .Title }} - {{ .Site.Title }}</title>
            <script src="https://cdn.tailwindcss.com"></script>
        </head>
        <body class="bg-gray-50 min-h-screen">
            {{ partial "header.html" . }}

            <main class="max-w-4xl mx-auto px-4 py-12">
                <article class="bg-white rounded-2xl shadow-xl p-8">
                    <header class="mb-8">
                        <h1 class="text-4xl font-bold mb-4">{{ .Title }}</h1>
                        <div class="flex items-center text-gray-600 mb-4">
                            <span class="mr-4">{{ .Date.Format "2006-01-02" }}</span>
                            <span>🕐 {{ .ReadingTime }} мин чтения</span>
                        </div>
                        {{ if .Params.image }}
                        <img src="{{ .Params.image }}" alt="{{ .Title }}" class="w-full h-64 object-cover rounded-lg mb-6">
                        {{ end }}
                    </header>
                    
                    <div class="prose prose-lg max-w-none">
                        {{ .Content }}
                    </div>

                    <footer class="mt-12 pt-6 border-t">
                        <div class="flex flex-wrap gap-2">
                            {{ range .Params.tags }}
                            <span class="bg-gray-100 text-gray-700 px-3 py-1 rounded-full text-sm">
                                {{ . }}
                            </span>
                            {{ end }}
                        </div>
                    </footer>
                </article>
            </main>

            {{ partial "footer.html" . }}
        </body>
        </html>
        EOF

        # Main index layout
        cat > layouts/index.html << 'EOF'
        <!DOCTYPE html>
        <html lang="ru">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>{{ .Site.Title }}</title>
            <script src="https://cdn.tailwindcss.com"></script>
        </head>
        <body class="bg-gray-50 min-h-screen">
            {{ partial "header.html" . }}

            <!-- Hero Section -->
            <section class="bg-gradient-to-br from-blue-600 via-purple-600 to-pink-600 text-white py-20">
                <div class="max-w-4xl mx-auto text-center px-4">
                    <h1 class="text-4xl md:text-6xl font-bold mb-6">🤖 AI Портфолио</h1>
                    <p class="text-xl md:text-2xl mb-8">Статьи и изображения, сгенерированные искусственным интеллектом</p>
                    <div class="flex justify-center space-x-4">
                        <a href="/articles/" class="bg-white text-blue-600 px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition">
                            📖 Все статьи
                        </a>
                        <a href="/gallery/" class="border-2 border-white text-white px-8 py-3 rounded-lg font-semibold hover:bg-white hover:text-blue-600 transition">
                            🎨 Галерея
                        </a>
                    </div>
                </div>
            </section>

            <!-- Latest Article -->
            <section class="py-16">
                <div class="max-w-6xl mx-auto px-4">
                    <h2 class="text-3xl font-bold text-center mb-12">📝 Последняя статья</h2>
                    {{ $latest := where site.RegularPages "Section" "posts" | first 1 }}
                    {{ if $latest }}
                    {{ range $latest }}
                    <div class="bg-white rounded-2xl shadow-xl overflow-hidden cursor-pointer" onclick="window.location.href='{{ .Permalink }}'">
                        <img src="{{ .Params.image }}" alt="{{ .Title }}" class="w-full h-64 object-cover">
                        <div class="p-8">
                            <div class="flex items-center mb-4">
                                <span class="bg-blue-100 text-blue-800 text-sm px-3 py-1 rounded-full">
                                    {{ .Date.Format "2006-01-02" }}
                                </span>
                            </div>
                            <h3 class="text-2xl font-bold mb-4">{{ .Title }}</h3>
                            <p class="text-gray-600 mb-6 leading-relaxed">{{ .Description }}</p>
                            <div class="flex items-center justify-between">
                                <div class="flex space-x-2">
                                    {{ range .Params.tags }}
                                    <span class="bg-gray-100 text-gray-700 px-3 py-1 rounded-full text-sm">
                                        {{ . }}
                                    </span>
                                    {{ end }}
                                </div>
                                <span class="bg-blue-600 text-white px-6 py-2 rounded-lg">
                                    Читать статью →
                                </span>
                            </div>
                        </div>
                    </div>
                    {{ end }}
                    {{ else }}
                    <div class="text-center py-12">
                        <p class="text-gray-600 text-xl">Статьи пока отсутствуют. Первая статья появится после запуска генерации.</p>
                    </div>
                    {{ end }}
                </div>
            </section>

            {{ partial "footer.html" . }}
        </body>
        </html>
        EOF

        # Articles list layout
        cat > layouts/articles/list.html << 'EOF'
        <!DOCTYPE html>
        <html lang="ru">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Статьи - {{ .Site.Title }}</title>
            <script src="https://cdn.tailwindcss.com"></script>
        </head>
        <body class="bg-gray-50 min-h-screen">
            {{ partial "header.html" . }}

            <main class="max-w-7xl mx-auto px-4 py-12">
                <h1 class="text-4xl font-bold text-center mb-12">📚 Все статьи</h1>
                
                {{ $articles := where .Site.RegularPages "Section" "posts" }}
                {{ if $articles }}
                <div class="grid grid-cols-1 md:grid-cols 2 lg:grid-cols-3 gap-8">
                    {{ range $articles }}
                    <article class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition cursor-pointer" onclick="window.location.href='{{ .Permalink }}'">
                        <img src="{{ .Params.image }}" alt="{{ .Title }}" class="w-full h-48 object-cover">
                        <div class="p-6">
                            <h2 class="text-xl font-bold mb-3">{{ .Title }}</h2>
                            <p class="text-gray-600 mb-4">{{ .Description }}</p>
                            <div class="flex items-center justify-between">
                                <span class="text-sm text-gray-500">{{ .Date.Format "2006-01-02" }}</span>
                                <span class="text-blue-600 font-semibold">Читать →</span>
                            </div>
                        </div>
                    </article>
                    {{ end }}
                </div>
                {{ else }}
                <div class="text-center py-12">
                    <p class="text-gray-600 text-xl">Статьи пока отсутствуют. Первая статья появится после запуска генерации.</p>
                </div>
                {{ end }}
            </main>

            {{ partial "footer.html" . }}
        </body>
        </html>
        EOF

        # Gallery layout
        cat > layouts/gallery/list.html << 'EOF'
        <!DOCTYPE html>
        <html lang="ru">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Галерея - {{ .Site.Title }}</title>
            <script src="https://cdn.tailwindcss.com"></script>
        </head>
        <body class="bg-gray-50 min-h-screen">
            {{ partial "header.html" . }}

            <main class="max-w-7xl mx-auto px-4 py-12">
                <h1 class="text-4xl font-bold text-center mb-12">🖼️ Галерея изображений</h1>
                
                {{ $gallery := .Site.Data.gallery }}
                {{ if $gallery }}
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                    {{ range $gallery }}
                    <div class="bg-white rounded-lg shadow-md overflow-hidden group">
                        <img src="{{ .src }}" alt="{{ .alt }}" class="w-full h-64 object-cover group-hover:scale-105 transition duration-300">
                        <div class="p-4">
                            <h3 class="font-semibold mb-2">{{ .title }}</h3>
                            <p class="text-sm text-gray-600">{{ .date }}</p>
                        </div>
                    </div>
                    {{ end }}
                </div>
                {{ else }}
                <div class="text-center py-12">
                    <p class="text-gray-600 text-xl">Галерея пуста. Новые изображения появятся после генерации статей.</p>
                </div>
                {{ end }}
            </main>

            {{ partial "footer.html" . }}
        </body>
        </html>
        EOF

        # About layout
        cat > layouts/about/single.html << 'EOF'
        <!DOCTYPE html>
        <html lang="ru">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Обо мне - {{ .Site.Title }}</title>
            <script src="https://cdn.tailwindcss.com"></script>
        </head>
        <body class="bg-gray-50 min-h-screen">
            {{ partial "header.html" . }}

            <main class="max-w-4xl mx-auto px-4 py-12">
                <div class="bg-white rounded-2xl shadow-xl p-8">
                    <h1 class="text-4xl font-bold text-center mb-8">👋 Обо мне</h1>
                    <div class="prose prose-lg max-w-none">
                        {{ .Content }}
                    </div>
                </div>
            </main>

            {{ partial "footer.html" . }}
        </body>
        </html>
        EOF

    - name: Create content pages
      run: |
        # About page
        cat > content/about.md << 'EOF'
        ---
        title: "Обо мне"
        date: 2024-01-01
        draft: false
        ---

        ## 👋 Привет! Я AI энтузиаст

        Я создаю контент с помощью современных технологий искусственного интеллекта.

        ### 🚀 Технологии:

        - **GPT-4** для генерации статей
        - **Stable Diffusion** для изображений  
        - **Hugo** для сайта
        - **GitHub Actions** для автоматизации

        ### 📊 Статистика:

        - 📝 Статей: {{ len (where .Site.RegularPages "Section" "posts") }}
        - 🖼️ Изображений: {{ len .Site.Data.gallery }}

        *Сайт сгенерирован искусственным интеллектом.*
        EOF

        # Articles list page
        cat > content/articles.md << 'EOF'
        ---
        title: "Все статьи"
        ---

        ## 📚 Все статьи

        Здесь собраны все сгенерированные AI статьи.
        EOF

        # Gallery page
        cat > content/gallery.md << 'EOF'
        ---
        title: "Галерея изображений"
        ---

        ## 🖼️ Галерея изображений

        Коллекция изображений, сгенерированных искусственным интеллектом.
        EOF

    - name: Generate article and image
      run: python .github/scripts/generate_content.py
      env:
        OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        CLOUDFLARE_API_KEY: ${{ secrets.CLOUDFLARE_API_KEY }}

    - name: Commit generated content
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .
        git commit -m "🤖 Auto-generated content [CI]" || echo "No changes to commit"
        git push

    - name: Setup Hugo
      uses: peaceiris/actions-hugo@v2
      with:
        hugo-version: '0.120.4'
        extended: true

    - name: Build Hugo site
      run: hugo --minify --logLevel info

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_branch: gh-pages
        publish_dir: ./public
        force_orphan: true
        keep_files: false
        enable_jekyll: false
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
