name: Content Generator

on:
  schedule:
    - cron: "0 6 * * *"   # каждый день в 06:00 UTC
  workflow_dispatch:       # запуск вручную

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout репозиторий
        uses: actions/checkout@v3

      - name: 🐍 Установка Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install requests python-slugify pyyaml

      - name: 🚀 Запуск генератора контента
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          CLOUDFLARE_API_KEY: ${{ secrets.CLOUDFLARE_API_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          python3 generate_content.py

      - name: 📝 Коммит и пуш изменений
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add content/posts/ static/images/posts/ data/gallery.yaml
          git commit -m "📝 Автоматическая генерация статьи и изображений [CI]" || echo "Нет изменений"
          git push
